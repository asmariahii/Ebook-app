import 'package:flutter/material.dart';

class NavSearchPage extends StatelessWidget {
  const NavSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      child: Center(child: Text("search page")),
    );
  }
}
